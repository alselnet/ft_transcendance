export const Main = () => {
    let main = document.createElement("main"); //Création d'un élément de type <main>

    main.id = "section" //Attribution d'un identifiant à l'élément <main>
    return main
}